# Copiar a .env y ajustar si el backend usa otro path (ej. .../api/)
VITE_URL_API=https://resimamis.onrender.com/

# Opcional: si el login devuelve idRol pero no el texto `rol`, definir el id numérico del rol COORDINADORA en BD.
# VITE_COORDINADORA_ID_ROL=2
